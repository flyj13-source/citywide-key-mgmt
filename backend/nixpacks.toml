[phases.setup]
nixPkgs = ["nodejs_22", "python3", "gcc", "gnumake"]

[phases.install]
cmds = ["npm ci --build-from-source"]

[phases.build]
cmds = ["npm run build", "npm run seed"]

[start]
cmd = "node dist/index.js"
