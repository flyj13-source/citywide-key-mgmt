# Local development — leave blank, Vite proxy handles /api/* → localhost:3001
VITE_API_URL=

# Production (Railway) — set to your deployed backend URL, no trailing slash
# VITE_API_URL=https://citywide-backend.up.railway.app
